# ELF Automation Mobile App

React Native (Expo) mobile app for legal intake, referral, and case management.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start Expo:
   ```bash
   npm start
   ``` 